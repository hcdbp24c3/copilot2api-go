services:
  copilot-go:
    image: ghcr.io/starrykira/copilot2api-go:latest
    container_name: copilot-go
    restart: unless-stopped
    ports:
      - "4141:4141"   # Dashboard + Proxy
    volumes:
      - copilot-data:/root/.local/share/copilot-api
    environment:
      - TZ=Asia/Shanghai

volumes:
  copilot-data:
    driver: local
